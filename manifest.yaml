name: starter-tool-plugin
label:
  en_US: Starter Tool Plugin
  ja_JP: スターターツールプラグイン
description:
  en_US: A minimal Dify Tool plugin scaffold.
  ja_JP: 最小構成の Dify ツールプラグインの雛形です。
author: your-name
version: "0.1.0"
type: tool
tags: [starter, example]

# NOTE: The exact manifest schema may evolve.
# Adjust keys to match the latest Dify docs as needed.
input_schema:
  type: object
  properties:
    text:
      type: string
      description: Input text to echo back
  required: []

runtime:
  language: python
  # Entry in form "<path>:<callable>" used by dev CLI; align with Dify spec as needed.
  entry: src/execute.py:execute
